# install package using these commands

> yarn add express typeorm reflect-metadata mysql2 multer jsonwebtoken bcrypt dotenv
> yarn add -D typescript ts-node ts-node-dev @types/node @types/express @types/multer @types/jsonwebtoken @types/bcrypt
